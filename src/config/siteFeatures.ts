/**
 * Public site search (header icon, `/search` page, sitemap URLs).
 * The Payload search plugin stays enabled; flip this to `true` when you want the UI back.
 */
export const enablePublicSiteSearch = false

/**
 * Footer theme selector (light / dark / auto). When false, it is not rendered.
 */
export const themeSelector = false
